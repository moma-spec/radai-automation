# Copy this file to .env and fill in your values.
# .env is gitignored — your token will never be committed.

# GitHub Personal Access Token (Fine-grained, read-only Contents on radai-automation-src)
# Get one at: https://github.com/settings/tokens?type=beta
GITHUB_TOKEN=

GITHUB_USER=moma-spec
GITHUB_REPO=radai-automation-src
SCRIPT_FILE=v3_dose.py

# Set to 1 to skip the update check and use your local cached script
SKIP_UPDATE=0
